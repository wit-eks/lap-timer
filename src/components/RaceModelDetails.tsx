import { Typography } from '@mui/material';
import React from 'react';

export function RaceModelDetails() {
  return (
    <React.Fragment>
      <Typography>some details</Typography>
    </React.Fragment>
  );
}
